<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Customization of the application theme for light mode. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Normal text and background -->
        <item name="android:colorBackground">@color/light</item>
        <item name="colorOnBackground">@color/dark</item>
        <item name="android:textColorSecondary">@color/normalFont</item>

        <!-- Elevated text and background, e. g. CardView -->
        <item name="colorSurface">@color/lightBackground</item>
        <item name="colorOnSurface">@color/normalFont</item>

        <!-- Primary color is used by the tool bar and status bar -->
        <item name="colorPrimary">@color/mediumBackground</item>
        <item name="colorPrimaryDark">@color/darkBackground</item>
        <item name="colorOnPrimary">@color/light</item>

        <!-- Secondary color is used by buttons and links, variant is used for TAN / ATC -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/dark</item>
        <item name="colorOnSecondary">@color/light</item>

        <!-- Errors and QR code scanner (red) -->
        <item name="colorError">@color/red</item>
        <item name="colorOnError">@color/light</item>
    </style>

</resources>
